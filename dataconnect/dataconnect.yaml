specVersion: "v1beta"
serviceId: "ndiambour"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ndiambour-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
